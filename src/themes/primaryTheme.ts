import React from "react";

export const theme = {
    colors: {
        primary: '#019387',
        secondary: '#703141',
        grey: '#aaa',
        white: '#fff',
        black: '#333',
    },
};

export const ThemeContext = React.createContext(theme);